[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn tugas_paas.wsgi"
healthcheckPath = "/health/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
